[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "smoke: lightweight tests that run on Mac (no GPU, no model loading)",
]
filterwarnings = [
    "ignore::DeprecationWarning",
]

[tool.mypy]
ignore_missing_imports = true

[tool.ruff]
line-length = 108
