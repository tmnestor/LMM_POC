```bash
git init
git add .
git commit -m "initial commit"
git remote add origin git@gitlab.sdpaap.aws.prod.atohnet.gov.au:du-lmm/lmm-poc.git
git checkout -b feature/hf-data-parallel
git push -u origin feature/hf-data-parallel
```