# Generated prompt for bank_statement using internvl3
# Generated at: 2025-09-11T07:45:32.403518
# Source: unified_schema.yaml 5.0-unified
# Field count: 5

metadata:
  document_type: bank_statement
  model: internvl3
  field_count: 5
  generated_at: '2025-09-11T07:45:32.403518'
  source: unified_schema.yaml 5.0-unified
field_list:
- DOCUMENT_TYPE
- STATEMENT_DATE_RANGE
- LINE_ITEM_DESCRIPTIONS
- TRANSACTION_DATES
- TRANSACTION_AMOUNTS_PAID
complete_prompt: 'CRITICAL: Pay extreme attention to EXACT digits and numbers.

  STEP 1: Carefully examine the entire document image.

  STEP 2: Identify the document type and key sections.

  STEP 3: Extract EXACTLY {field_count} fields with PRECISE digit accuracy.


  DIGIT PRECISION & VISUAL EXTRACTION INSTRUCTIONS:

  - ⚠️ CRITICAL: Extract EVERY digit EXACTLY as shown - do not skip or modify digits

  - ⚠️ CRITICAL: For multi-digit numbers, carefully copy ALL digits in the correct sequence

  - FIRST: Scan the document header/top section for business name and ABN

  - SECOND: Look for customer/payer information (usually in the middle section)

  - THIRD: Find the itemized list or transaction table - count ALL rows

  - FOURTH: Locate the totals section (usually at the bottom)

  - Output each field EXACTLY as shown below

  - Start your response with DOCUMENT_TYPE: [value]

  - End your response with TRANSACTION_AMOUNTS_PAID: [value]


  EXTRACT THESE 5 FIELDS IN THIS EXACT ORDER:


  DOCUMENT_TYPE: [Look for words like INVOICE, RECEIPT, TAX INVOICE, STATEMENT at the top - answer with INVOICE/RECEIPT/STATEMENT
  or NOT_FOUND]

  STATEMENT_DATE_RANGE: [Find ''Statement Period:'', ''Period:'', or date range at top of statement (e.g. ''1 Jan - 31 Jan'')
  or NOT_FOUND]

  LINE_ITEM_DESCRIPTIONS: [Find the MAIN TABLE/LIST in middle of document - extract ALL product/service names using | character
  ONLY - format: item1 | item2 | item3 - do NOT use commas or NOT_FOUND]

  TRANSACTION_DATES: MANDATORY ROW COUNT MATCHING: Count total table rows in the transaction table. Your pipe-separated list
  MUST have exactly the same number of elements. Go through each table row sequentially: Row 1 - if date cell has value include
  it, if empty write NOT_FOUND. Row 2 - if date cell has value include it, if empty write NOT_FOUND. Continue for all rows.
  DO NOT skip empty rows or compress the list.

  TRANSACTION_AMOUNTS_PAID: MANDATORY ROW COUNT MATCHING: Count total table rows in the transaction table. Your pipe-separated
  list MUST have exactly the same number of elements. Go through each table row sequentially: Row 1 - if amount cell has value
  include it, if empty write NOT_FOUND. Row 2 - if amount cell has value include it, if empty write NOT_FOUND. Continue for
  all rows. DO NOT skip empty rows or compress the list.


  CRITICAL EXTRACTION RULES - FOCUS ON DIGIT ACCURACY:

  - Each line must be: FIELDNAME: value

  - 🔢 NUMBERS: Copy digits EXACTLY - do not round, truncate, or skip any digits

  - 🔢 ABN: Must be EXACTLY 11 digits - count them! (e.g., ''12 345 678 901'' format)

  - 🔢 ADDRESSES: Copy house/building numbers EXACTLY (e.g., ''123'' not ''1123'')

  - 🔢 PRICES: Include ALL items even if prices repeat (e.g., if two items cost $15.00, list both)

  - 🔢 TRANSACTION LISTS: Count the rows in tables - your list must match the actual number of rows found

  - IMPORTANT: For SUPPLIER_NAME, look at the business/company name at the TOP of the document

  - IMPORTANT: For LINE_ITEM_DESCRIPTIONS, list ALL items shown in the itemized section

  - For prices, distinguish between unit price (per item) and total price (quantity × unit)

  - Use ''NOT_FOUND'' ONLY when the field truly doesn''t exist in the document

  - For transaction lists: go through each table row - if cell has value include it, if empty write NOT_FOUND

  - Example transaction format: ''Value1 | NOT_FOUND | Value3'' (one element per table row)

  - For boolean IS_GST_INCLUDED: answer ''true'' if GST amount is shown, ''false'' if not

  - Extract monetary values with 2 decimal places (e.g., ''$12.50'' or ''12.50'')


  OUTPUT EXACTLY 5 LINES. Stop after TRANSACTION_AMOUNTS_PAID.'
field_instructions:
  DOCUMENT_TYPE: '[Look for words like INVOICE, RECEIPT, TAX INVOICE, STATEMENT at the top - answer with INVOICE/RECEIPT/STATEMENT
    or NOT_FOUND]'
  STATEMENT_DATE_RANGE: '[Find ''Statement Period:'', ''Period:'', or date range at top of statement (e.g. ''1 Jan - 31 Jan'')
    or NOT_FOUND]'
  LINE_ITEM_DESCRIPTIONS: '[Find the MAIN TABLE/LIST in middle of document - extract ALL product/service names using | character
    ONLY - format: item1 | item2 | item3 - do NOT use commas or NOT_FOUND]'
  TRANSACTION_DATES: 'MANDATORY ROW COUNT MATCHING: Count total table rows in the transaction table. Your pipe-separated list
    MUST have exactly the same number of elements. Go through each table row sequentially: Row 1 - if date cell has value
    include it, if empty write NOT_FOUND. Row 2 - if date cell has value include it, if empty write NOT_FOUND. Continue for
    all rows. DO NOT skip empty rows or compress the list.'
  TRANSACTION_AMOUNTS_PAID: 'MANDATORY ROW COUNT MATCHING: Count total table rows in the transaction table. Your pipe-separated
    list MUST have exactly the same number of elements. Go through each table row sequentially: Row 1 - if amount cell has
    value include it, if empty write NOT_FOUND. Row 2 - if amount cell has value include it, if empty write NOT_FOUND. Continue
    for all rows. DO NOT skip empty rows or compress the list.'
