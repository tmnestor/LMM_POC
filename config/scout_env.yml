name: LMM_POC_LLAMA4SCOUT
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - transformers>=4.51
      - compressed-tensors
      - accelerate
      - pillow
      - pyyaml
      - rich
      - typer
      - tqdm
      - einops
      - safetensors
      - psutil
# POST-INSTALL (run in order after `conda env create -f config/scout_env.yml`):
#   conda activate LMM_POC_LLAMA4SCOUT
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124  # must match nvcc toolkit (12.4), not driver (13.0)
#   pip install flash-attn --no-build-isolation --no-cache-dir  # --no-cache-dir avoids cross-device link error on NFS
prefix: /home/jovyan/.conda/envs/LMM_POC_LLAMA4SCOUT
