name: LMM_POC_LLAMA4SCOUT
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch --index-url https://download.pytorch.org/whl/cu124
      - torchvision --index-url https://download.pytorch.org/whl/cu124
      - transformers>=4.51
      - compressed-tensors
      - accelerate
      - pillow
      - pyyaml
      - rich
      - typer
      - tqdm
      - einops
      - safetensors
      - psutil
# NOTE: Install flash-attn AFTER env creation (requires torch at build time):
#   conda activate LMM_POC_LLAMA4SCOUT && pip install flash-attn
prefix: /home/jovyan/.conda/envs/LMM_POC_LLAMA4SCOUT
